# Copy to .env and set your own value. The SA password is read by docker-compose
# when the database container is created; .env is gitignored, .env.example is not.
#
# SQL Server rejects weak passwords: at least 8 characters, with three of
# uppercase, lowercase, digits and symbols.
MSSQL_SA_PASSWORD=change-me-before-first-run
